# Approval behavior

Request a narrowly scoped rule or permission only when the active client supports that request and it is necessary for the authorized task. Avoid broad executable prefixes or wildcard grants. Verify the returned decision before using the capability.
