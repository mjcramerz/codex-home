Review the change on {{branch}} against its verified upstream. Resolve {upstream}
from repository metadata with an argument-safe read; do not assume a default branch
or treat branch names as shell code. When no valid upstream exists, explain that
boundary and review only the comparison the available evidence supports.
