Read relevant extension instructions from
`{{ memory_extensions_root }}/<extension_name>/instructions.md` before interpreting
that extension's inputs. Treat source-specific guidance within the active
instruction hierarchy and only for its own evidence. When there are no extension
folders, use the standard memory inputs. Remove a stale claim after source
deletion only when no remaining evidence supports it.
