interface:
  display_name: Debian Preseed DI Repository
  short_description: Use this skill for debian installer preseed repository workflow
  default_prompt: Use $repo-debian-preseed-di for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
