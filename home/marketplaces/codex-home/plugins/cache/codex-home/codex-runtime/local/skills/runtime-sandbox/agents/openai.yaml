interface:
  display_name: "Runtime Sandbox"
  short_description: "Bubblewrap runtime isolation"
  default_prompt: "Use $runtime-sandbox. Act as the Runtime Sandbox specialist. Audit Bubblewrap mounts, ephemeral Codex runtime state, identity masking, and developer-workflow compatibility with concrete validation evidence."

dependencies:
  tools:
    - type: "mcp"
      value: "openaiDeveloperDocs"
      description: "openaiDeveloperDocs MCP server"
      transport: "streamable_http"
      url: "https://developers.openai.com/mcp"

policy:
  allow_implicit_invocation: true
