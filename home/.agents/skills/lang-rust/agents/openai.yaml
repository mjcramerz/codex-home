interface:
  display_name: "LANG-Rust"
  short_description: "Build Rust workspaces with cargo defaults"
  icon_small: "./assets/icon-32.png"
  icon_large: "./assets/icon-128.png"
  brand_color: "#C44A2A"
  default_prompt: "Use $lang-rust. Act as the \"LANG-Rust\" specialist for \"Build Rust workspaces with cargo defaults\". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks."

policy:
  allow_implicit_invocation: true
