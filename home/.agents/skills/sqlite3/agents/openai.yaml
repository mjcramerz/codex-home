interface:
  display_name: "DB-SQLite3"
  short_description: "Operate SQLite databases safely from the sqlite3 CLI"
  icon_small: "./assets/icon-32.png"
  icon_large: "./assets/icon-128.png"
  brand_color: "#0F766E"
  default_prompt: "Use $sqlite3. Act as the \"DB-SQLite3\" specialist for \"Operate SQLite databases safely from the sqlite3 CLI\". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks."

policy:
  allow_implicit_invocation: true
