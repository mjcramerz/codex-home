interface:
  display_name: AUDIT-Installer Regression
  short_description: Audit installer flows for regressions and permission drift
  icon_small: ./assets/icon-32.png
  icon_large: ./assets/icon-128.png
  brand_color: '#2563EB'
  default_prompt: Use $installer-regression-audit for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
