interface:
  display_name: Google Calendar
  short_description: Manage Google Calendar schedules and events
  icon_small: ./assets/icon.png
  icon_large: ./assets/logo.png
  default_prompt: Use $google-calendar for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
