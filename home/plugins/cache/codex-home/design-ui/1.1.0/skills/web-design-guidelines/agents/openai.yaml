interface:
  display_name: Web Design Guidelines
  short_description: 'Review UI code for Web Interface Guidelines compliance. Use when asked to "review my UI", '
  default_prompt: Use $web-design-guidelines for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
