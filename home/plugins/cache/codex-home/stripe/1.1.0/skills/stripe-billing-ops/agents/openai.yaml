interface:
  display_name: Stripe Billing Ops
  short_description: Use this skill for stripe billing operations, invoicing, and customer-impact reviews
  brand_color: '#4F46E5'
  default_prompt: Use $stripe-billing-ops for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
dependencies:
  tools:
  - type: mcp
    value: stripe
    description: stripe MCP server
    transport: streamable_http
    url: https://mcp.stripe.com
policy:
  allow_implicit_invocation: true
