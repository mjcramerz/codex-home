interface:
  display_name: "Stripe Billing Ops"
  short_description: "Stripe billing operations, invoicing, and customer-impact reviews"
  brand_color: "#4F46E5"
  default_prompt: "Use $stripe-billing-ops. Act as the Stripe Billing Ops specialist. Use current evidence, keep changes reviewable, and report assumptions and validation clearly."

dependencies:
  tools:
    - type: "mcp"
      value: "stripe"
      description: "stripe MCP server"
      transport: "streamable_http"
      url: "https://mcp.stripe.com"

policy:
  allow_implicit_invocation: true
