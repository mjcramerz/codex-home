interface:
  display_name: Jina Reader Research
  short_description: Use Jina Reader-style workflows to extract, compare, and summarize implementation-relevant
  brand_color: '#0EA5E9'
  default_prompt: Use $jina-reader-research for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
dependencies:
  tools:
  - type: mcp
    value: jina-ai
    description: jina-ai MCP server
    transport: streamable_http
    url: https://mcp.jina.ai/v1
policy:
  allow_implicit_invocation: true
