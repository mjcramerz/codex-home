interface:
  display_name: Develop Web Game
  short_description: Use this skill for web game dev + Playwright test loop
  icon_small: ./assets/game-small.svg
  icon_large: ./assets/game.png
  default_prompt: Use $develop-web-game for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
dependencies:
  tools:
  - type: mcp
    value: jina-ai
    description: jina-ai MCP server
    transport: streamable_http
    url: https://mcp.jina.ai/v1
