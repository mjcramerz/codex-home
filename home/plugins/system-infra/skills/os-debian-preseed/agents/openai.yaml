interface:
  display_name: OS-Debian Preseed
  short_description: Create Debian preseed files for unattended installations
  icon_small: ./assets/icon-32.png
  icon_large: ./assets/icon-128.png
  brand_color: '#3265CC'
  default_prompt: Use $os-debian-preseed for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
