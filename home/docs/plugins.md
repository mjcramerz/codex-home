# Route plugin work

Use [the plugin contract](operations/PLUGINS.md) when you select, edit or synchronize a plugin. Read only the matching manifest and skill; discover actual tools before invoking them.

Keep installed state, local bundled source, authentication and authorization separate. Preserve explicit invocation constraints, resource paths and license notices. Do not preload caches or infer capabilities from a plugin name.
