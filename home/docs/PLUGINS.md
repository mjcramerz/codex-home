# Plugin entrypoint

Use `operations/PLUGINS.md` for plugin trust, cache lifecycle and bounded-hook
behavior. Existing source mirrors and manifests are retained. Catalog consistency
does not establish the security, authentication or availability of an external
plugin. The versioned browser-service path must match the actual installed app.
