interface:
  display_name: DATA-Config Formats
  short_description: Use this skill for edit JSON, YAML, and TOML safely with schema awareness
  icon_small: ./assets/icon-32.png
  icon_large: ./assets/icon-128.png
  brand_color: '#F59E0B'
  default_prompt: Use $data-json for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
