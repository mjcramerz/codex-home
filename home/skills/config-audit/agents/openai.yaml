interface:
  display_name: AUDIT-Config
  short_description: Audit config merge order, placeholders, and policy drift
  icon_small: ./assets/icon-32.png
  icon_large: ./assets/icon-128.png
  brand_color: '#0F766E'
  default_prompt: Use $config-audit for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
