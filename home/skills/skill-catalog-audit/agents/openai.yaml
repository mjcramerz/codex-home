interface:
  display_name: AUDIT-Skill Catalog
  short_description: Audit skill metadata, routing coverage, and bundle mapping
  icon_small: ./assets/icon-32.png
  icon_large: ./assets/icon-128.png
  brand_color: '#14B8A6'
  default_prompt: Use $skill-catalog-audit for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
