interface:
  display_name: "CLI-Design"
  short_description: "Design CLI contracts, flags, and exit semantics cleanly"
  icon_small: "./assets/icon-32.png"
  icon_large: "./assets/icon-128.png"
  brand_color: "#DC2626"
  default_prompt: "Use $cli-design. Act as the \"CLI-Design\" specialist for \"Design CLI contracts, flags, and exit semantics cleanly\". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks."

policy:
  allow_implicit_invocation: true
