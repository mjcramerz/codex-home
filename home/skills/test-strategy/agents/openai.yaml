interface:
  display_name: TEST-Strategy
  short_description: Use this skill for map code changes to focused tests and regression checks
  icon_small: ./assets/icon-32.png
  icon_large: ./assets/icon-128.png
  brand_color: '#10B981'
  default_prompt: Use $test-strategy for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
