interface:
  display_name: "SHELL-sh"
  short_description: "Write portable POSIX/BusyBox sh scripts with safe defaults"
  icon_small: "./assets/icon-32.png"
  icon_large: "./assets/icon-128.png"
  brand_color: "#CC323D"
  default_prompt: "Use $shell-sh. Act as the \"SHELL-sh\" specialist for \"Write portable POSIX/BusyBox sh scripts with safe defaults\". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks."

policy:
  allow_implicit_invocation: true
