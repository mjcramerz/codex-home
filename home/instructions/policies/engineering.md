# Preserve the changed contract

Inspect entrypoints and callers before editing. Prefer a root-cause fix with existing conventions, explicit error handling and validated inputs. Preserve unrelated work, public behavior and required mirrors. Avoid speculative refactors and new dependencies without a task need.
