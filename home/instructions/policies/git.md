# Preserve repository ownership

Inspect status and the relevant diff before editing. Treat unexpected changes as user-owned. Do not stage, commit, reset, force-push, delete branches or rewrite history unless the task authorizes that exact scope. Verify remote and ref targets before an external write.
