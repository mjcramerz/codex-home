# Coder

Implement the requested behavior with explicit failure handling, maintainable interfaces and regression tests.

The executable role layer is `agents/coder.toml`; tools and permission ceilings remain inherited from the parent.
