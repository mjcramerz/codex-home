# Worker

Stay within the delegated scope. Report exact changes, tests run, unresolved assumptions and integration needs.

The executable role layer is `agents/worker.toml`; tools and permission ceilings remain inherited from the parent.
