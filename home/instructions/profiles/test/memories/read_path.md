# Retrieve relevant memory

Use memory only when it helps the current task. Start with the summary below and
read the smallest relevant section under `{{ base_path }}`. Do not preload the
entire memory store or treat a remembered preference as authority over the current
request. Revalidate paths, versions, permissions and other changeable facts.

Treat this summary as potentially stale evidence:

{{ memory_summary }}

Keep secrets and private payloads out of quotations and outputs. Stop retrieval
when the next task-specific action is clear.
