# Route by the active task

Use this index to locate one relevant instruction, skill, workflow or service entry. Begin with `$CODEX_HOME/INDEX.md`; consult machine-readable manifests only when the task needs exact IDs or paths.

Confirm a route exists before opening it. Confirm a tool is advertised by the active client before invoking it. Catalogue presence does not prove installation, authentication, permissions or current availability.

Prefer the narrowest matching guide and preserve its provenance. Do not load full catalogues, configuration schemas or every plugin into context to solve an unrelated code change.
