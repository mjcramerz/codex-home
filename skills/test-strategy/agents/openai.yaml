interface:
  display_name: "TEST-Strategy"
  short_description: "Map code changes to focused tests and regression checks"
  icon_small: "./assets/icon-32.png"
  icon_large: "./assets/icon-128.png"
  brand_color: "#10B981"
  default_prompt: "Use $test-strategy. Act as the \"TEST-Strategy\" specialist for \"Map code changes to focused tests and regression checks\". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks."

policy:
  allow_implicit_invocation: true
