interface:
  display_name: "PARSER-Design"
  short_description: "Design parsers, grammars, and AST transforms safely"
  icon_small: "./assets/icon-32.png"
  icon_large: "./assets/icon-128.png"
  brand_color: "#8B5CF6"
  default_prompt: "Use $parser-design. Act as the \"PARSER-Design\" specialist for \"Design parsers, grammars, and AST transforms safely\". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks."

policy:
  allow_implicit_invocation: true
