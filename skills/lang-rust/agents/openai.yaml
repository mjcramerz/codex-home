interface:
  display_name: LANG-Rust
  short_description: Build Rust workspaces with cargo defaults
  icon_small: ./assets/icon-32.png
  icon_large: ./assets/icon-128.png
  brand_color: '#C44A2A'
  default_prompt: Use $lang-rust for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
