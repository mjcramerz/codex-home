interface:
  display_name: "AUDIT-Config"
  short_description: "Audit config merge order, placeholders, and policy drift"
  icon_small: "./assets/icon-32.png"
  icon_large: "./assets/icon-128.png"
  brand_color: "#0F766E"
  default_prompt: "Use $config-audit. Act as the \"AUDIT-Config\" specialist for \"Audit config merge order, placeholders, and policy drift\". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks."

policy:
  allow_implicit_invocation: true
