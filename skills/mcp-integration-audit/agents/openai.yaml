interface:
  display_name: AUDIT-MCP Integration
  short_description: Audit MCP auth, timeouts, and dependency wiring
  icon_small: ./assets/icon-32.png
  icon_large: ./assets/icon-128.png
  brand_color: '#F97316'
  default_prompt: Use $mcp-integration-audit for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
