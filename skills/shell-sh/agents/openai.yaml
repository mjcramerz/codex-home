interface:
  display_name: SHELL-sh
  short_description: Use this skill for write portable POSIX/BusyBox sh scripts with safe defaults
  icon_small: ./assets/icon-32.png
  icon_large: ./assets/icon-128.png
  brand_color: '#CC323D'
  default_prompt: Use $shell-sh for the current matching task. Read its entrypoint and only the resources needed for the next action. Follow the active tool contract, preserve user scope and unrelated work, protect secrets, and report actual results and limitations.
policy:
  allow_implicit_invocation: true
