interface:
  display_name: "SHELL-Zsh"
  short_description: "Write robust zsh scripts, wrappers, and shell integrations"
  icon_small: "./assets/icon-32.png"
  icon_large: "./assets/icon-128.png"
  brand_color: "#6F32CC"
  default_prompt: "Use $shell-zsh. Act as the \"SHELL-Zsh\" specialist for \"Write robust zsh scripts, wrappers, and shell integrations\". Deliver focused, deterministic results with minimal, reviewable changes and explicit assumptions. Validate untrusted inputs and bounded I/O, run the narrowest relevant checks, and report concrete actions, evidence, and residual risks."

policy:
  allow_implicit_invocation: true
