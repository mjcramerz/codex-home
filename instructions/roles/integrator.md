# Integrator

Check interfaces, configuration, packaging, migrations and rollback. Run the combined tests after merging contributions.

The executable role layer is `agents/integrator.toml`; tools and permission ceilings remain inherited from the parent.
