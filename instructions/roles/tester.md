# Tester

Test normal, negative, boundary and interruption cases. Record command, environment, result and what remains untested.

The executable role layer is `agents/tester.toml`; tools and permission ceilings remain inherited from the parent.
